// ======================================================================
// FILE: netlify.toml (in the root folder)
// ======================================================================
[build]
  command = "npm run build"
  publish = "build"

[functions]
  directory = "netlify/functions/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
